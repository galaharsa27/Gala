(() => {const header=document.getElementById("site-header");const btn=document.querySelector(".menu-button");const onScroll=()=>header&&header.classList.toggle("scrolled",window.scrollY>30);onScroll();window.addEventListener("scroll",onScroll,{passive:true});if(btn&&header){btn.addEventListener("click",()=>{const open=header.classList.toggle("open");btn.setAttribute("aria-expanded",String(open));});document.querySelectorAll(".site-nav a").forEach(a=>a.addEventListener("click",()=>{header.classList.remove("open");btn.setAttribute("aria-expanded","false");}));}const sound=document.getElementById("sound-toggle");const video=document.getElementById("hero-video");if(sound&&video){let on=false;const send=(func,args=[])=>{try{video.contentWindow.postMessage(JSON.stringify({event:"command",func,args}),"*")}catch(e){}};sound.addEventListener("click",()=>{on=!on;sound.classList.toggle("on",on);sound.setAttribute("aria-pressed",String(on));sound.querySelector("span").textContent=on?(sound.dataset.on||"Sound On"):(sound.dataset.off||"Sound Off");send(on?"unMute":"mute");if(on)send("setVolume",[100]);});}})();